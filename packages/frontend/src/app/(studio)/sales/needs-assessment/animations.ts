// Re-exported from shared wizard design system — see src/lib/wizard/
export {
    slideInRight, slideInLeft, fadeInUp, fadeIn,
    chipBounce, checkPop, selectPulse, shimmer,
    subtleFloat, scaleIn, pulseRing,
    drift1, drift2, drift3,
    glowPulse, filmStagger,
} from "@/lib/wizard/animations";
