'use client';

import React from 'react';
import { Typography, Box } from '@mui/material';

export default function CoverageScenesTab() {
    return (
        <Box>
            <Typography variant="h6" gutterBottom>
                Coverage Scenes
            </Typography>
            <Typography variant="body2" color="textSecondary">
                Manage coverage scenes for video deliverables (Coming Soon)
            </Typography>
        </Box>
    );
}
