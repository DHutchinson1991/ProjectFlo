# Shared Types

Ambient type declarations and infrastructure types consumed by `shared/` internals. Scoping rules in `frontend-architecture.instructions.md` § "Shared layer rules".

## Key files
| File | Purpose |
|------|--------|
| `css.d.ts` | Module declaration allowing CSS file imports in TypeScript |
