/**
 * Re-exports from @projectflo/shared (the single source of truth).
 * All hours-summation helpers are defined once in packages/shared/src/hours.ts.
 */
export {
  sumEffortHours,
  sumEstimatedHours,
  sumTotalHours,
} from '@projectflo/shared';
