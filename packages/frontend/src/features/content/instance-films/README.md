# Instance Films

## What this module does
Placeholder feature for instance-level film operations (project-specific film instances derived from package film templates). API folder exists but has no bindings yet.

## Key files
| File | Purpose |
|------|---------|
| `api/` | Empty — endpoints not yet implemented on frontend |

## Related modules
- **Backend**: `content/instance-films` — controller + services (fully implemented)
- **Frontend**: `content/films` — parent film feature
